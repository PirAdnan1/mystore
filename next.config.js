/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['fakestoreapi.com/products'], // Add the domain of the API here
  },
};
module.exports = nextConfig 
